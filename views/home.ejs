<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlashLink - URL Shortener</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 50px; }
        input { padding: 10px; width: 300px; font-size: 16px; }
        button { padding: 10px 20px; font-size: 16px; cursor: pointer; background-color: #007bff; color: white; border: none; }
        .result { margin-top: 20px; padding: 15px; background-color: #e9ecef; display: inline-block; border-radius: 5px; }
        table { margin: 40px auto; border-collapse: collapse; width: 80%; text-align: left; }
        th, td { padding: 12px; border: 1px solid #ddd; }
        th { background-color: #f2f2f2; }
    </style>
</head>
<body>
    <h1>🔗 FlashLink URL Shortener</h1>
    
    <% if (locals.id) { %>
        <div class="result">
            <p><strong>Success! Short URL Generated:</strong></p>
            <a href="http://localhost:8001/<%= id %>" target="_blank">http://localhost:8001/<%= id %></a>
        </div>
    <% } %>

    <div style="margin-top: 30px;">
        <form action="/url" method="POST">
            <label>Enter Your Original URL:</label><br><br>
            <input type="text" name="url" placeholder="https://example.com" required>
            <button type="submit">Shorten</button>
        </form>
    </div>

    <% if (locals.urls && locals.urls.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>S. No</th>
                    <th>Short ID</th>
                    <th>Original Link</th>
                    <th>Total Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach((url, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= url.shortId %></td>
                        <td style="word-break: break-all;"><%= url.redirectURL %></td>
                        <td style="font-weight: bold; text-align: center;"><%= url.visitHistory.length %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</body>
</html>